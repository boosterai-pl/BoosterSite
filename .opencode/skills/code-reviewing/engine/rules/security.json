[
  {
    "slug": "sec-auth-bypass",
    "severity": "critical",
    "description": "Verify no endpoint skips auth guards without explicit `@Public()` decorator and justification."
  },
  {
    "slug": "sec-rbac-missing",
    "severity": "critical",
    "description": "Check that RBAC roles and attribute-based filters are enforced server-side in the API layer."
  },
  {
    "slug": "sec-frontend-only-permission",
    "severity": "critical",
    "description": "Flag any permission check that exists only in frontend code."
  },
  {
    "slug": "sec-rate-limit-missing",
    "severity": "warning",
    "description": "Verify rate limiting on authentication, password reset, and other sensitive endpoints."
  },
  {
    "slug": "sec-unsafe-sql",
    "severity": "critical",
    "description": "Flag string concatenation or interpolation in SQL - only `postgres` tagged templates with auto-parameterization are safe."
  },
  {
    "slug": "sec-dynamic-identifier",
    "severity": "critical",
    "description": "Confirm dynamic column/table names use allowlists, never user input directly."
  },
  {
    "slug": "sec-data-exposure",
    "severity": "critical",
    "description": "Check that API responses do not include sensitive data the client schema does not require."
  },
  {
    "slug": "sec-feature-flag-client-only",
    "severity": "warning",
    "description": "Verify feature flags are checked server-side, not only in UI rendering."
  },
  {
    "slug": "sec-error-leak",
    "severity": "warning",
    "description": "Flag error responses that expose stack traces or internal system details."
  },
  {
    "slug": "sec-secrets-in-logs",
    "severity": "warning",
    "description": "Flag logging statements that might leak secrets, tokens, or full request bodies."
  },
  {
    "slug": "sec-r2-unsigned",
    "severity": "critical",
    "description": "Check that S3/storage access uses signed URLs with explicit TTL."
  }
]
