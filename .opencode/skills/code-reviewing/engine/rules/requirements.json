[
  {
    "slug": "req-boundary-violation",
    "severity": "critical",
    "description": "Verify business logic lives in NestJS services, not in React page components or frontend code."
  },
  {
    "slug": "req-side-effect-in-api",
    "severity": "critical",
    "description": "External side effects (CRM, Slack, email, exports) go through workers via BullMQ queue."
  },
  {
    "slug": "req-transition-client-side",
    "severity": "critical",
    "description": "Reservation state transitions (`FREE -> OPTION -> RESERVED -> SOLD`) enforced server-side only."
  },
  {
    "slug": "req-tech-stack-divergence",
    "severity": "warning",
    "description": "Flag new dependencies or approaches that diverge from `docs/TECH_STACK.md` without justification."
  },
  {
    "slug": "req-spec-drift",
    "severity": "warning",
    "description": "If the PR claims to fix a bug, verify the code actually addresses the root cause."
  },
  {
    "slug": "req-scope-mismatch",
    "severity": "warning",
    "description": "If the PR adds a feature, verify the implementation matches the described scope."
  },
  {
    "slug": "req-scope-creep",
    "severity": "warning",
    "description": "Flag scope creep - changes unrelated to the stated purpose belong in separate PRs."
  },
  {
    "slug": "req-clickup-scope",
    "severity": "warning",
    "description": "Compare the ClickUp task description and acceptance criteria against the PR's actual changes. Flag significant scope gaps where the PR clearly misses stated acceptance criteria or implements something materially different from the ticket. Do not flag minor wording differences."
  },
  {
    "slug": "req-clickup-status",
    "severity": "warning",
    "description": "If the ticket is already marked as done/closed but this PR is still open, flag as warning - possible stale PR or ticket updated prematurely."
  },
  {
    "slug": "req-feature-doc-deviation",
    "severity": "warning",
    "description": "When reviewing feature code, check existing `docs/features/` for how the feature should work. Flag deviations from documented design unless the docs are also updated in the same PR."
  },
  {
    "slug": "req-docs-ahead-of-code",
    "severity": "warning",
    "description": "Flag PRs that update feature docs without corresponding code changes - docs should not drift ahead of implementation."
  },
  {
    "slug": "req-migration-replay-safety",
    "severity": "critical",
    "description": "If a PR modifies `packages/db/drizzle/` or `packages/db/src/schema/`, verify migrations are tested against a pre-seeded database state (main schema + main seed -> new migration). Missing replay validation blocks merge because staging migrations run on existing rows, not empty databases."
  },
  {
    "slug": "req-dev-seed-alignment",
    "severity": "critical",
    "description": "If schema or migration behavior changes, verify `packages/db/src/seed/dev-seed.ts` (and its tests) stay aligned with the new schema and constraints. Missing alignment blocks merge because staging migrations run on existing seeded rows."
  },
  {
    "slug": "req-unarchived-change",
    "severity": "critical",
    "description": "Check if the PR modifies files in `openspec/changes/<name>/` (especially `tasks.md` with checked-off tasks). If implementation tasks are complete but the change directory is NOT moved to `openspec/changes/archive/YYYY-MM-DD-<name>/`, the archive step was skipped."
  },
  {
    "slug": "req-spec-sync-skipped",
    "severity": "warning",
    "description": "Check for delta specs at `openspec/changes/<name>/specs/` that should have been synced to `openspec/specs/<capability>/spec.md`. If deltas exist in the active change but the canonical specs were not updated, spec sync was skipped."
  },
  {
    "slug": "req-pr1-no-archive",
    "severity": "warning",
    "description": "If the PR is a spec-only PR (PR1 - only adds/modifies OpenSpec artifacts, no runtime code), archival is NOT expected. Archival happens in the implementation PR (PR2)."
  },
  {
    "slug": "req-no-change-link",
    "severity": "warning",
    "description": "If the PR does not touch any OpenSpec change files and is not linked to a change, this check does not apply."
  },
  {
    "slug": "req-dto-from-contracts",
    "severity": "warning",
    "description": "DTOs and request/response schemas must be imported from `@ooh/contracts`, not defined locally in `apps/api/` or `apps/worker/`."
  },
  {
    "slug": "req-web-inline-endpoint",
    "severity": "critical",
    "description": "Ad-hoc endpoint definitions (`{ path: '...', method: '...', requestSchema: z.object({}) }`) defined inline in `apps/web/` instead of importing from `@ooh/contracts`. All endpoint definitions must live in `packages/contracts/`. Inline definitions bypass contract type safety and break when the API changes."
  }
]
